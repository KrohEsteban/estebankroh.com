---
import experience from '../data/experience.json';
---

<section>
  <h2>Experiencia</h2>
  {experience.map(e => (
    <div class="exp-card">
      <h3>{e.title}</h3>
      <p><em>{e.company} – {e.period}</em></p>
      <p>{e.description}</p>
      <ul>
        {e.responsibilities.map(r => <li>🎯 {r}</li>)}
      </ul>
      <ul>
        {e.achievements.map(a => <li>🏆 {a}</li>)}
      </ul>
    </div>
  ))}
</section>
